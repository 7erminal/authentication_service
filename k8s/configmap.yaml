apiVersion: v1
kind: ConfigMap
metadata:
  name: naki-authentication-config
  namespace: naki
data:
  DB_HOST: "postgres-platform.nakilink.postgres.database.azure.com"
  DB_PORT: "5432"
  DB_NAME: "auth"
  APP_HTTP_PORT: "5080"
  BEEGO_RUNMODE: "prod"
