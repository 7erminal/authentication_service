apiVersion: v1
kind: Service
metadata:
  name: naki-authentication-service
  namespace: naki
spec:
  selector:
    app: naki-authentication-service
  ports:
    - name: http
      port: 80
      targetPort: 5080
      protocol: TCP
  type: ClusterIP
